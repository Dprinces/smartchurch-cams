{
  "name": "smartchurch-cams",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed-cac.ts && npm run build",
    "start": "node server/dist/server.js"
  }
}
